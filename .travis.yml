language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
before_install:
  - rustup component add rustfmt
  - rustup component add clippy
script:
  - cargo fmt --verbose
  - cargo clippy --verbose
  - cargo build --verbose
  - cargo test --verbose